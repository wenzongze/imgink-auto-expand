# img.ink 图床自动扩容 —— 部署与图形化操作指南

> 适用环境：QNAP NAS（QTS 系统），通过 Container Station / Python3 运行。
> 配套文件：`sign_expand.py`（扩容脚本）、`web_app.py`（图形化面板）、`start_web.sh`（保活/自启）、`config.ini`（账号配置）、`notify.py`（邮件通知）。

---

## 一、功能说明

`sign_expand.py` 是一个 Python 脚本，用于**每天自动为 img.ink 图床账户增加存储空间（扩容一次）**。

- 自动登录 img.ink 账户（账户/密码写在 `config.ini`）。
- 访问扩容页面，完成每日一次的免费扩容。
- 支持**随机延迟**，模仿真人操作（避免被风控）。
- 执行结果通过邮件通知（QQ 邮箱 SMTP）。
- 可由 **Web 图形化面板**统一管理：查看状态、手动立即执行、暂停/启用、查看运行日志、修改面板标题。

### 文件清单与用途

| 文件 | 用途 |
|------|------|
| `sign_expand.py` | 核心扩容脚本（登录 + 扩容 + 通知） |
| `config.ini` | 账号、邮箱、扩容时段配置 |
| `notify.py` | 邮件通知模块（被 sign_expand.py 调用） |
| `web_app.py` | Flask 图形化面板（端口 9999） |
| `start_web.sh` | 面板保活 + 开机自启脚本 |
| `requirements.txt` | Python 依赖（requests、flask） |

---

## 二、前置条件

1. **QNAP NAS** 已开启 SSH（控制台 → 网络与文件服务 → Telnet/SSH → 启用 SSH）。
2. 已安装 **Python3 QPKG**（Container Station 或 App Center 里的 Python3）。
   Python3 路径各机型不同，本指南以 QNAP 常见路径为例（**请替换为你自己的路径**）：
   ```
   /share/CACHEDEV1_DATA/.qpkg/Python3/opt/python3/bin/python3
   ```
   若路径不同，全文出现该路径处均须替换为你的实际路径（可用 `which python3` 或 `find / -name python3 2>/dev/null` 查找）。
3. 一个 **img.ink 账号**（账户 + 密码）。
4. 一个 **QQ 邮箱**（用于接收执行结果通知，需开启 SMTP 并获取授权码）。

---

## 三、部署到 NAS

### 步骤 1：上传文件到 NAS

将本仓库文件上传到 NAS 的任意目录，例如（**路径可自定义**）：
```
/share/CACHEDEV1_DATA/Web/gadget/auto_sign
```
下文统一用变量 `DEPLOY_DIR` 指代该目录。若你放在别处，把所有 `/share/CACHEDEV1_DATA/Web/gadget/auto_sign` 替换成你的实际路径即可。
（可用 File Station 拖拽，或 `scp` / `rsync` 上传。）

### 步骤 2：安装 Python 依赖

SSH 登录 NAS（用 `admin` 或有 sudo 权限的账户），执行：

```bash
PY="你的python3路径"   # 例如 /share/CACHEDEV1_DATA/.qpkg/Python3/opt/python3/bin/python3
PIP="$PY -m pip"
$PIP install --user requests flask
```

> 若提示权限不足，先 `sudo su -` 切到 root，或确认 pip 的 `--user` 指向 `/share/homes/<你的用户>/.local`。

### 步骤 3：配置账号与邮箱

编辑 `config.ini`：

```ini
[ACCOUNT]
account  = 你的img.ink账号
password = 你的密码

[MAIL]
smtp_server = smtp.qq.com
smtp_port   = 465
sender      = 你的QQ邮箱@qq.com
auth_code   = 邮箱授权码（非登录密码）
receiver    = 接收通知的邮箱@qq.com

[SETTING]
random_start = 8      # 允许执行的最早小时（8 点）
random_end   = 22     # 允许执行的最晚小时（22 点）
login_url  = https://img.ink/auth/login.html
expand_url = https://img.ink/user/moremore.html
```

> **random_start / random_end**：脚本只在 `random_start:00 ~ random_end:00` 之间随机延迟执行，模拟真人。手动触发时（面板「立即执行」）跳过随机延迟。

### 步骤 4：测试脚本能否正常运行

```bash
cd $DEPLOY_DIR   # 即你的部署目录
PYTHONPATH=/share/homes/<你的用户>/.local/lib/python3.12/site-packages \
你的python3路径 sign_expand.py
```

应看到类似输出：
```
========================================
img.ink 自动扩容 - 启动
[Token] 获取成功: a1b2c3d4...
[登录] 成功（302 跳转）
[扩容] ✅ 扩容成功！
========================================
执行结束
========================================
```

并收到一封通知邮件，说明部署成功。

### 步骤 5：配置定时任务（cron）

让脚本每天自动跑。以 root 身份编辑 crontab：

```bash
# 先备份
cp /etc/config/crontab /etc/config/crontab.bak

# 用 vi 编辑（或 echo 追加一行）
vi /etc/config/crontab
```

追加（每天 07:00 触发，脚本内部再随机延迟到 8~22 点间；**请替换路径**）：
```
0 7 * * * 你的python3路径 $DEPLOY_DIR/sign_expand.py >> $DEPLOY_DIR/cron.log 2>&1
```

重载 cron：
```bash
/etc/init.d/crond.sh restart
```

> QNAP 的 crontab 文件位于 `/etc/config/crontab`，修改后必须 `crond.sh restart` 才生效。

### 步骤 6：启动图形化面板

```bash
/bin/sh $DEPLOY_DIR/start_web.sh
```

面板将在 `0.0.0.0:9999` 监听。浏览器访问：
```
http://<NAS_IP>:9999
```

---

## 四、图形化面板操作详解

打开 `http://<NAS_IP>:9999` 后，界面如下结构：

```
┌─────────────────────────────────────────────┐
│  🖼️ img.ink 自动扩容          [时间]          │  ← 标题（可点击改名）
├─────────────────────────────────────────────┤
│  [⏸ 暂停计划任务] [🔄 刷新] [🔍 页面自检] [♻️ 重启面板] │  ← 顶部操作按钮
├─────────────────────────────────────────────┤
│  📋 全部计划任务  (N 条)                       │
│  ⭐ 我的自动执行任务                           │
│    ┌─ 任务卡片 ─────────────────────────┐     │
│    │ 🖼️ img.ink 自动扩容  [自动] [7 0 * * *] │  │
│    │ 说明文字…                            │     │
│    │ [▶ 立即执行]  [📜 日志]              │     │
│    │ ┌─ 运行日志区 ─┐                     │     │
│    └───────────────┘                     │     │
│  ⚙️ 系统任务（只读）  ▾ 收起               │
└─────────────────────────────────────────────┘

（右下角悬浮按钮）
        🌙  ← 切换日间/夜间
        ⬆️  ← 回到顶部
```

### 1. 修改面板标题

- 鼠标移到顶部标题 `🖼️ img.ink 自动扩容`，会出现蓝色 `✎`。
- **点击标题** → 变成输入框。
- 输入新标题，按 `Enter` 或点「保存」。
- 标题持久化到 `title.txt`，重启面板后保留。

### 2. 修改任务名称（点击自定义）

- 每个任务卡片的任务名（如「🪟 微软积分自动获取」）**可点击改名**。
- **点击任务名** → 变成输入框，自带圆角美化的「保存 / 取消」按钮（紫色保存、灰色取消）。
- 输入新名称，按 `Enter` 或点「保存」；`Esc` 或点「取消」放弃。
- 名称按任务的**完整命令**作为唯一标识持久化到 `jobnames.json`，重启面板后保留。
- 清空名称则恢复默认显示。
- 说明：任务名下方仍会正常展示该任务的完整命令行（如 `> .../nas.log 2>&1`），**那一行是命令预览，属正常显示**；任务名本身不应出现命令残片。

### 3. 暂停 / 启用计划任务

- 点 `⏸ 暂停计划任务`：写入 `disabled.flag`，脚本检测到后当天跳过执行（手动「立即执行」仍可用）。
- 按钮变为 `▶ 启用计划任务`，再点恢复。

### 4. 立即执行（手动扩容）

- 在「我的自动执行任务」卡片中点 `▶ 立即执行`。
- 脚本以**手动模式**运行，**跳过随机延迟**，立即登录并扩容。
- 执行中按钮显示 `⏳ 执行中...`，完成后自动展开下方日志区。

### 5. 查看运行日志

- 点卡片上的 `📜 日志` 按钮 → 展开日志区，显示该任务最近 200 行日志。
  - img.ink 任务日志文件：`auto_sign/cron.log`
- 日志区可再次点 `📜 日志` 收起。

### 6. 刷新 / 重启面板

- `🔄 刷新`：重新拉取任务状态。
- `♻️ 重启面板`：**上传新 `web_app.py` 后直接点此按钮即可生效，无需 SSH**。
  - 新逻辑：先在后台杀掉所有旧 `web_app.py` 进程，再用 `setsid` 直接拉起磁盘上的当前 `web_app.py`，**不再依赖 `start_web.sh` 的保活判断**（旧逻辑存在竞态：延迟拉起期间旧进程还在，`start_web.sh` 判定「进程已存在」便不拉新进程，导致重启失效）。
  - 重启约 4 秒完成，页面会自动刷新。浏览器建议用**新建标签页**或 `Ctrl+Shift+R` 强制刷新确认。

### 7. 页面自检

- 顶部操作区有 `🔍 页面自检` 按钮。
- 点击后展开结果框，逐项显示（✅/❌）：
  1. 任务名编辑框样式（`job-name-editor`）是否加载
  2. 保存/取消按钮样式（`.btn-mini`）是否注入
  3. 服务器最新页面是否含新样式（直接重新拉取 `location.href`，绕过浏览器缓存）
  4. 本地 DOM 与服务器是否一致（不一致说明浏览器在用旧标签/缓存）
  5. 后端接口 `/api/status` 是否可用
- 末尾给出结论性提示（全绿 / 服务器旧 / 本地旧 / 接口异常），无需再靠 F12 手动验证。
- 适用场景：改完代码重启后，快速确认样式与功能是否真的生效（尤其当浏览器扩展向页面注入额外 `<style>` 标签、干扰 `document.querySelector('style')` 判断时）。

### 8. 悬浮按钮（右下角）

- `🌙 / ☀️`：切换日间 / 夜间模式（localStorage 记忆）。
- `⬆️`：回到页面顶部。
- 行为：**向下滚动时隐藏，向上滚动时出现**（顶部附近自动隐藏「回到顶部」）。

### 9. 系统任务区

- 「⚙️ 系统任务（只读）」为 QNAP 系统自带 cron（系统更新、应用检查等），仅展示不可操作。

---

## 五、常见问题

| 现象 | 原因 / 解决 |
|------|------------|
| 页面打不开 | 确认面板进程已运行；`ps aux \| grep web_app.py` 看进程；`netstat -tlnp \| grep 9999` 看端口；防火墙放行 9999 |
| 点「重启面板」没反应 / 重启后还是旧页面 | 已修复：新逻辑不再依赖 `start_web.sh` 保活判断，改为「先杀全部旧进程 + setsid 直接拉起当前文件」。若仍异常，用底部「完整重启命令」手动重启一次 |
| 任务名里出现命令残片（如 `> .../nas.log 2>&1")">保存`） | 已修复：任务名 onclick 不再拼接完整命令，command 改从全局数组 `window.__cronJobs[gIdx]` 取。刷新后消失。任务名下方的命令预览行属正常显示 |
| 浏览器改动不生效 | **强制刷新** `Ctrl+Shift+R`（Mac `Cmd+Shift+R`），或新建标签页重新打开；也可点「🔍 页面自检」确认本地 DOM 与服务器是否一致 |
| 页面自检某项 ❌ | 看结论提示：服务器旧→点「♻️ 重启面板」；本地旧→强制刷新/新建标签；接口异常→检查进程是否存活 |
| 扩容提示"今日已扩容" | 正常，每天仅一次有效 |
| 登录失败 | 检查 `config.ini` 账号密码；网站可能改版导致 token 提取失败 |
| 收不到邮件 | 检查 QQ 邮箱授权码、SMTP 端口（465 SSL） |

---

## 六、完整重启命令（备用）

若面板异常、或点「♻️ 重启面板」按钮仍不生效，SSH 手动重启（直接 setsid 拉起，不经 `start_web.sh`）：

```bash
PY="你的python3路径"   # 例如 /share/CACHEDEV1_DATA/.qpkg/Python3/opt/python3/bin/python3
DIR="你的部署目录"      # 例如 /share/CACHEDEV1_DATA/Web/gadget/auto_sign

# 杀掉所有旧面板进程
ps aux | grep "[w]eb_app.py" | awk '{print $2}' | xargs -r kill
sleep 2

# 直接拉起当前文件（后台常驻）
setsid "$PY" "$DIR/web_app.py" >> "$DIR/web.log" 2>&1 &

sleep 2
# 确认已监听 9999
netstat -tlnp 2>/dev/null | grep 9999
```

> QNAP BusyBox 环境无 `nohup`/`pkill`，用 `kill` + `setsid ... &` 实现后台常驻。
