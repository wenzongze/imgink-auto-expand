# 更新与维护指南

本文档说明如何把本仓库的改动更新到 NAS、以及如何同步到 Gitee / GitHub 双平台。

## 一、仓库地址（访问地址）

| 平台 | 地址 |
|------|------|
| Gitee | https://gitee.com/cloud_moon_yarn/imgink-auto-expand |
| GitHub | https://github.com/wenzongze/imgink-auto-expand |

**Web 管理面板访问地址**（部署在 NAS 上后）：
```
http://<你的NAS内网IP>:9999
```
例如 `http://192.168.1.100:9999`。面板自身无鉴权，仅建议在信任的内网环境暴露；如需公网访问，请自行加反代 + 鉴权。

---

## 二、本地双远程配置

仓库克隆到本地后，已配置两个远程：

```powershell
git remote -v
# origin   -> https://gitee.com/cloud_moon_yarn/imgink-auto-expand (Gitee)
# github   -> https://github.com/wenzongze/imgink-auto-expand      (GitHub)
```

若你是从零克隆，可手动添加：

```powershell
git remote add origin  https://gitee.com/cloud_moon_yarn/imgink-auto-expand.git
git remote add github  https://github.com/wenzongze/imgink-auto-expand.git
```

---

## 三、日常更新代码并推送到双平台

修改代码后，在本地仓库目录执行：

```powershell
# 1. 暂存并提交
git add -A
git commit -m "描述你的改动"

# 2. 推送到 Gitee
git push origin master

# 3. 推送到 GitHub
git push github master
```

> 两个平台分支名均为 `master`。若想一次推两个，可用 `git push --all`（需两个远程都配置好）。

---

## 四、把更新同步到 NAS 实际运行环境

NAS 上的运行文件通常与 git 仓库是同一份（建议直接把仓库克隆到 NAS 部署目录）。更新流程：

```bash
cd /path/to/your/deploy/dir   # 即 web_app.py 所在目录
git pull origin master        # 拉取最新代码
# 若 NAS 同时跟踪 GitHub：
git pull github master

# 重启面板使新代码生效（在 Web 面板点「♻️ 重启面板」按钮，
# 或在 NAS 命令行执行 start_web.sh / 手动 kill 旧进程后 setsid 拉起）
```

> ⚠️ 隐私文件（`config.ini`、`*.log` 等）已在 `.gitignore` 排除，不会随 pull 覆盖你的本地配置。

---

## 五、首次部署简要回顾（详见《部署与图形化操作指南.md》）

1. 把仓库克隆/拷贝到 NAS。
2. 复制 `config.example.ini` 为 `config.ini` 并填入你的 img.ink 账号与邮件通知配置。
3. 安装依赖：`pip3 install -r requirements.txt`。
4. 启动：`bash start_web.sh`（或 `setsid python3 web_app.py >> web.log 2>&1 &`）。
5. 浏览器打开 `http://<NAS_IP>:9999` 使用面板。

---

## 六、常见问题

- **改了代码面板没变？** 点面板「🔍 页面自检」确认是否生效；若仍旧，用「♻️ 重启面板」或命令行重启进程。
- **推送被拒绝？** 先 `git pull` 合并远端改动再 push。
- **隐私泄露风险？** 切勿把 `config.ini`、任何 `*.log` 加入提交；它们已在 `.gitignore` 中。
